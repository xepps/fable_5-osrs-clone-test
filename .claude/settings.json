{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '\\.(ts|tsx)$' && cd \"$CLAUDE_PROJECT_DIR\" && npm run -s typecheck || true"
          }
        ]
      }
    ]
  }
}
